
# 🤖 LangChain Gemini Project Template

โปรเจกต์นี้เป็นโครงสร้างพื้นฐาน (Template) สำหรับการพัฒนา LLM Application ด้วย LangChain และ Google Gemini โดยถูกออกแบบมาให้เป็นระเบียบ (Modular) ง่ายต่อการขยายผล และพร้อมรันบน Docker ทันที

## 📁 โครงสร้างโปรเจกต์ (Project Structure)
โครงสร้างนี้เน้นการแยกส่วนหน้าที่ (Separation of Concerns) เพื่อให้จัดการ Code ได้ง่ายในระยะยาว:
```
├── src/
│   ├── chain/          # รวบรวมตรรกะการเชื่อมต่อ (Chains) ทั้งแบบ Stateless และ Stateful
│   ├── chat/           # Business Logic สำหรับการจัดการการโต้ตอบ (Stateless/Stateful)
│   ├── llm/            # การตั้งค่าโมเดล LLM (Gemini)
│   ├── prompt/         # จัดเก็บ Prompt Templates และ MessagesPlaceholder
│   └── main.py         # จุดรันโปรแกรมหลัก (Entry Point)
├── .dockerignore       # ป้องกันไฟล์ไม่จำเป็นเข้า Docker
├── .env                # เก็บข้อมูลความลับ (API Keys)
├── .gitignore          # ป้องกันการ Push ขยะและ .env ขึ้น GitHub
├── Dockerfile          # คำสั่งสำหรับ Build Image
├── docker-compose.yml  # การตั้งค่า Container Orchestration
└── requirement.txt     # รายการ Dependencies หลัก
```
## 🛠 วิธีการติดตั้งและรัน (Installation & Setup)

1. เตรียม API Key
สร้างไฟล์ .env ไว้ที่ Root ของโปรเจกต์
ใส่ API Key ของ Google Gemini:

```bash
GOOGLE_API_KEY=your_actual_api_key_here
```

2. การรันผ่าน Docker (แนะนำ)
โปรเจกต์นี้ตั้งค่า docker-compose.yml ให้รองรับการทำงานแบบ Interactive และ Hot-Reload (ผ่าน Volumes):

```bash
# สั่งรัน container แบบ Interactive เพื่อใช้ input() ได้
docker compose run --rm basic_langchain
```

3. การรันใน Local Environment
หากต้องการรันโดยไม่ใช้ Docker:

```bash
pip install -r requirement.txt
python -m src.main
```